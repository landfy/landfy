{
  "name": "landfy",
  "version": "0.1.0",
  "scripts": {
    "lint": "standard --fix",
    "test": "npm run lint && jest --coverage"
  },
  "standard": {
    "globals": [
      "beforeAll",
      "afterAll",
      "beforeEach",
      "afterEach",
      "describe",
      "it",
      "test",
      "expect"
    ]
  },
  "license": "MIT",
  "dependencies": {
    "commander": "^2.12.2"
  },
  "devDependencies": {
    "coveralls": "^2.13.1",
    "jest": "^20.0.4",
    "standard": "^10.0.2"
  },
  "bin": "./bin/landfy"
}
